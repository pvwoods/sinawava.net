
extends layout

block prepend title
  #{ page.title } -

block header
  include author
  a.back(href='/') << back to index
  h1= page.title
  p
    | written by:
    mixin author(page.metadata.author)

block content  
  div#postRight
    h1
      a(href=page.url)= page.title
        div#postContent!=page.html
